[project]
name = "aslan-ozaslan"
version = "0.1.0"
description = "Aslan Özaslan sürdürülebilir tahmin altyapısı"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
