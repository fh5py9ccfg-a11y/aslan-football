FROM python:3.11-slim
WORKDIR /app

COPY pyproject.toml /app/
COPY apps/worker /app/apps/worker

RUN pip install --no-cache-dir -e .

ENV PYTHONPATH=/app/apps/worker
CMD ["python", "-m", "worker_app.sync_main"]
